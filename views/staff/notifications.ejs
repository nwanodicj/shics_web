<%# AUTO-COMMENT: File Overview - views/staff/notifications.ejs | Purpose: View template: server-rendered UI markup and bindings. %>
<h2>Your Notifications</h2>

<ul>
  <% notifications.forEach(note => { %>
    <li><%= note.message %></li>
  <% }) %>
</ul>